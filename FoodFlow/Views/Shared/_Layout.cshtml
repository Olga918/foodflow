<!DOCTYPE html>
<html lang="en">
@{
    var currentController = (ViewContext.RouteData.Values["controller"]?.ToString() ?? "").ToLowerInvariant();
    var currentAction = (ViewContext.RouteData.Values["action"]?.ToString() ?? "").ToLowerInvariant();
    string NavClass(string controller, string? action = null)
    {
        controller = controller.ToLowerInvariant();
        action = action?.ToLowerInvariant();
        var isActive = action is null
            ? currentController == controller
            : currentController == controller && currentAction == action;
        return isActive ? "nav-link active" : "nav-link";
    }
}
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - FoodFlow</title>
    <script type="importmap"></script>
    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css" />
    <link rel="stylesheet" href="~/css/site.css" asp-append-version="true" />
    <link rel="stylesheet" href="~/FoodFlow.styles.css" asp-append-version="true" />
</head>
<body>
    <header>
        <nav class="navbar navbar-expand-lg ff-navbar shadow-sm">
            <div class="container">
                <a class="navbar-brand fw-bold" asp-area="" asp-controller="Home" asp-action="Index">FoodFlow</a>
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#ffNav" aria-controls="ffNav"
                        aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div id="ffNav" class="collapse navbar-collapse">
                    <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                        <li class="nav-item">
                            <a class="@NavClass("Home", "Index")" asp-area="" asp-controller="Home" asp-action="Index">Home</a>
                        </li>
                        <li class="nav-item">
                            <a class="@NavClass("Menu", "Index")" asp-area="" asp-controller="Menu" asp-action="Index">Menu</a>
                        </li>
                        <li class="nav-item">
                            <a class="@NavClass("Cart", "Index")" asp-area="" asp-controller="Cart" asp-action="Index">Cart</a>
                        </li>
                        <li class="nav-item">
                            <a class="@NavClass("Home", "Contact")" asp-area="" asp-controller="Home" asp-action="Contact">Contact</a>
                        </li>
                        @if (User.IsInRole("Admin"))
                        {
                            <li class="nav-item">
                                <a class="@NavClass("Admin", "Index")" asp-area="" asp-controller="Admin" asp-action="Index">Admin</a>
                            </li>
                            <li class="nav-item">
                                <a class="@NavClass("Purchases", "Index")" asp-area="" asp-controller="Purchases" asp-action="Index">Purchases</a>
                            </li>
                        }
                        @if (User.IsInRole("Cook"))
                        {
                            <li class="nav-item">
                                <a class="@NavClass("Kitchen", "Index")" asp-area="" asp-controller="Kitchen" asp-action="Index">Kitchen</a>
                            </li>
                            <li class="nav-item">
                                <a class="@NavClass("Kitchen", "Prepare")" asp-area="" asp-controller="Kitchen" asp-action="Prepare">Line prep</a>
                            </li>
                        }
                        @if (User.IsInRole("Storekeeper"))
                        {
                            <li class="nav-item">
                                <a class="@NavClass("Stock", "Index")" asp-area="" asp-controller="Stock" asp-action="Index">Stock</a>
                            </li>


                        }
                    </ul>
                    <partial name="_LoginPartial" />
                </div>
            </div>
        </nav>
    </header>

    <main class="ff-main">
        <div class="container py-4">
            @RenderBody()
        </div>
    </main>

    <footer class="ff-footer border-top">
        <div class="container py-3 d-flex justify-content-between flex-wrap gap-2 align-items-center">
            <span>&copy; 2026 FoodFlow</span>
            <div class="d-flex flex-wrap gap-3">
                <a asp-area="" asp-controller="Home" asp-action="Contact">Contact</a>
                <a asp-area="" asp-controller="Home" asp-action="Privacy">Privacy</a>
            </div>
        </div>
    </footer>

    <script src="~/lib/jquery/dist/jquery.min.js"></script>
    <script src="~/lib/bootstrap/dist/js/bootstrap.bundle.min.js"></script>
    <script src="~/js/site.js" asp-append-version="true"></script>
    @await RenderSectionAsync("Scripts", required: false)
</body>
</html>
